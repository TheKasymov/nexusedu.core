# NexusEdu Core API 🚀

NexusEdu — это модульная B2B ERP/LMS платформа нового поколения для образовательных учреждений.

Архитектура системы спроектирована по принципам **Domain-Driven Design (DDD)**. Главная ценность продукта 
заключается в универсальном ядре, которое способно адаптироваться под бизнес-процессы абсолютно разных 
образовательных форматов (Школы, Детские сады, IT-курсы) без изменения базового исходного кода.

## 🛠 Технологический стек
* **Java 21 (LTS)**
* **Spring Boot 3.4.2**
* **PostgreSQL 16**
* **Spring Data JPA**
* **Docker & Docker Compose**
* **JUnit 5 & Mockito** (Unit-тестирование)
* **OpenAPI 3 / Swagger** (Документация)

## 🏗 Архитектурные особенности

1. **Динамическая конфигурация сущностей (JSONB)**
   Для обеспечения максимальной гибкости таблицы `Organization` используется тип данных `JSONB` в PostgreSQL. Это позволяет хранить специфичные настройки учреждений (например, время тихого часа для детского сада или расписание звонков для школы) без структурных изменений базы данных (schema-less подход).

2. **Паттерн "Стратегия" (Strategy Pattern)**
   Бизнес-логика обработки оценок и метрик инкапсулирована в отдельные модули с помощью паттерна Strategy (`SchoolGradingStrategy`, `KindergartenGradingStrategy`). Это избавляет систему от жестких конструкций `if/else` и соблюдает принцип Open/Closed из SOLID: при добавлении нового типа учреждения старый код не меняется.

3. **Событийно-ориентированная посещаемость**
   Модуль `AttendanceLog` спроектирован как универсальный шлюз, готовый принимать данные с различных устройств (ручной ввод, RFID-карты, AI-распознавание лиц).

## 🚀 Быстрый запуск

### 1. Запуск инфраструктуры (База данных)
Убедитесь, что у вас установлен Docker. В корневой директории выполните:

``` bash
docker-compose up -d
```

2. Сборка и запуск приложения
Проект использует Maven Wrapper, поэтому локальная установка Maven не требуется.

```Bash
./mvnw clean install -DskipTests
./mvnw spring-boot:run
Система автоматически создаст структуру таблиц и базовые тестовые данные (Admin, School, Kindergarten) при первом запуске через DataInitializer.
```
📖 Документация API (Swagger)
После успешного запуска приложения, интерактивная документация API доступна по адресу:
-> http://localhost:8080/swagger-ui/index.html

🧪 Тестирование
Проект покрыт Unit-тестами для проверки слоев контроллеров и бизнес-логики. Для запуска тестов выполните:

``` Bash
./mvnw test
```
